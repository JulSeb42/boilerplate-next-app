import { clsx } from "@julseb-lib/react"

export function {{>pascalName }}({ }: I{{>pascalName }}) {
    return (
        <{{ lowerCase tag }} className={clsx("", "{{>kebabName}}")}>
            
        </{{ lowerCase tag }}>
    )
}

interface I{{>pascalName }} {
	
}
