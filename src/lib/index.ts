// Main lib exports - client-safe only
export { default as axios } from "./axios"

// For server-side imports, use: import { ... } from "lib/server"
// For client-side imports, use: import { ... } from "lib" or "lib/client"
